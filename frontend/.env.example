# Base URL of the DemoForge backend (Mac Pro). Use your Mac Pro Tailscale IP when frontend runs on MacBook.
# When frontend and backend are on the same host (e.g. Mac Pro via SSH), 127.0.0.1 is fine.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
